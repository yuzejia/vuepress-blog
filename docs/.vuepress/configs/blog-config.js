const blogConfig = {
    category: {
        location: 1,     // 在导航栏菜单中所占的位置，默认2
        text: '前端' // 默认文案 “分类”
      },
      tag: {
        location: 3,     // 在导航栏菜单中所占的位置，默认3
        text: '标签页'      // 默认文案 “标签”
      }
}

module.exports = blogConfig;